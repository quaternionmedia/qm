<!-- SEED FILE: copy verbatim to the forking project's own repo root as
     CLAUDE.md (see README.md's "Forking a new project", step 5). -->
# CLAUDE.md

See [AGENTS.md](AGENTS.md) — this file exists only so tooling that reads
`CLAUDE.md` specifically still finds the same instructions. Do not
duplicate content here; edit `AGENTS.md`.
