<!-- SEED FILE: copy verbatim to the forking project's own repo root as
     .github/copilot-instructions.md (see README.md's "Forking a new
     project", step 5). -->
See [AGENTS.md](../AGENTS.md) at the repository root — this file exists
only so tooling that doesn't yet read `AGENTS.md` automatically still finds
the same instructions. Do not duplicate content here; edit `AGENTS.md`.
